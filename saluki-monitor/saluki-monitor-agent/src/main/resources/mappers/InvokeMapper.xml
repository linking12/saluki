<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quancheng.saluki.monitor.mapper.SalukiInvokeMapper">


	<sql id="allNoAsOfInvoke">
		id,
		invokeDate,
		invokeTime,
		service,
		method,
		consumer,
		provider,
		type,
		success,
		failure,
		elapsed,
		concurrent,
		maxInput,
		maxOutput,
		maxElapsed,
		maxConcurrent,
		input,
		output
	</sql>

	<insert id="addInvoke"
		parameterType="com.quancheng.saluki.monitor.SalukiInvoke">
		insert into
		saluki_invoke (
		<include refid="allNoAsOfInvoke" />
		)
		values(
		#{id},
		#{invokeDate},
		#{invokeTime},
		#{service},
		#{method},
		#{consumer},
		#{provider},
		#{type},
		#{success},
		#{failure},
		#{elapsed},
		#{concurrent},
		#{maxInput},
		#{maxOutput},
		#{maxElapsed},
		#{maxConcurrent},
		#{input},
		#{output}
		)
	</insert>

	<select id="queryDataByService" parameterType="java.util.Map"
		resultType="com.quancheng.saluki.monitor.SalukiInvokeStatistics">
		SELECT 
		  SUM(concurrent) / (SUM(success)+SUM(failure)) as tps,
		  (SUM(concurrent) / (SUM(success)+SUM(failure))) *(SUM(input)/1024) as kbps,
          SUM(elapsed) / (SUM(success)+SUM(failure)) as elapsed
		FROM saluki_invoke
		WHERE
		1 = 1
		<if test="service != null">
			AND service=#{service}
		</if>
		<if test="type != null">
			AND type=#{type}
		</if>
		GROUP BY invokeTime/1000
	</select>

	<delete id="truncateTable">
		truncate table saluki_invoke
	</delete>
</mapper>